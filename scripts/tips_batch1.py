# -*- coding: utf-8 -*-
import json

PATH = r"C:\Users\Jergarcia\Desktop\linux-plus-trainer\data\bank.json"
bank = json.load(open(PATH, encoding="utf-8"))
byidx = {q["_idx"]: q for q in bank}

TIPS = {
0: "docker run creates AND starts a new container in one step; start/exec both require a container that already exists, and build only produces an image \u2014 keep those four verbs straight.",
1: "Live migration needs the two hosts' CPUs to be instruction-set compatible (matching architecture) and both hosts need access to the same shared storage path \u2014 RAM/network speed/USB don't need to match exactly.",
2: "Don't trust a file's extension \u2014 check its actual type (e.g. with `file`) before picking an extraction tool; a \".bkp\" file can turn out to be a cpio archive rather than tar/gzip.",
3: "Ansible vocabulary: Inventory = hosts/groups, Playbook = tasks to run, Fact = data gathered from a host, Collection = a bundle of reusable content.",
4: "/proc is a virtual, in-memory filesystem generated by the kernel at boot \u2014 that's the tell whenever a question asks about \"non-persistent/volatile\" data.",
5: "getent passwd <user> pulls uid/home/shell from whatever user database is configured (files, LDAP, etc.) in one line; `id` only returns UID/GID info, not the home directory or shell.",
6: "Image subcommands: pull downloads an image from a registry; import/save/init aren't valid ways to fetch one.",
7: "${var} is brace-expansion for variable substitution; $(cmd) is command substitution \u2014 mixing the two up is a classic distractor on scripting questions.",
8: "tee reads stdin and writes to both the screen and a file at once \u2014 reach for it whenever a question wants you to watch output live *and* save it; plain redirection (> or >>) only does one or the other.",
9: "\"Consolidating events to a single location\" is the textbook definition of log aggregation \u2014 watch for that phrasing.",
10: "chattr +a sets the append-only attribute: the file can be added to but not overwritten or deleted, even by root, until the attribute is removed.",
11: "Passwordless SSH always needs a keypair first, then the public key copied to the target: ssh-keygen (generate) \u2192 ssh-copy-id (deploy). The other options invent flag combinations that don't exist.",
12: "netplan try applies a network config temporarily and auto-reverts if you don't confirm within the timeout \u2014 the safe choice for risky changes; netplan apply commits immediately with no rollback.",
13: "journald is systemd's own logging service \u2014 it collects and stores log data in a binary journal, viewed with journalctl.",
14: "usermod -aG (append + Groups) adds a supplementary group without disturbing the primary group; -g alone *replaces* the primary group instead \u2014 always pair -a with -G.",
15: "Python 3's extensibility through modules/packages is a core selling point \u2014 it's explicitly open source and not fully backward-compatible with Python 2.",
16: "\"Managing event records across multiple servers\" almost always points to centralized log aggregation on this exam, not per-server backups or rotation.",
17: "initrd/initramfs is a minimal filesystem holding just enough kernel modules/drivers to mount the real root filesystem during early boot \u2014 it's a boot-time bridge, not a security or SSH component.",
18: "renice adjusts a running process's scheduling priority (niceness) without killing it \u2014 the standard fix whenever one task is starving others of CPU.",
19: "LDAP is the protocol for querying a hierarchical directory of users/groups/machines/OUs \u2014 don't confuse it with authentication protocols like Kerberos or transport security like TLS.",
20: "shred overwrites a file or device with multiple passes of random data before removing it \u2014 the dedicated tool for secure erasure, stronger than a single dd pass or a plain rm.",
21: "OpenSCAP automates compliance scanning and remediation against a SCAP security policy \u2014 the answer whenever a question says \"automate compliance/vulnerability management.\"",
22: "dmidecode reads the DMI/SMBIOS tables for a full hardware inventory (board, BIOS, memory slots, etc.); lscpu/lsmod/dmesg are each narrower (CPU only, loaded modules only, kernel ring buffer).",
23: "dd makes a raw, bit-for-bit block-level copy \u2014 the only option here that duplicates a disk exactly, which forensic imaging requires; the others (cp/cpio/tar) are file-level and would miss unallocated/deleted data.",
24: "/etc/resolv.conf lists the DNS nameservers a system queries \u2014 the file to edit whenever a question is about setting/changing DNS servers.",
25: "dnf history shows past transactions (what was installed/updated/removed and when) \u2014 the tool for figuring out what changed during patching.",
26: "A shell variable only becomes part of the environment (visible to child processes) once you export it \u2014 plain VAR=value stays local to the current shell.",
27: "Always lint/review AI-generated infrastructure code before applying it \u2014 CompTIA's \"wrong practice\" distractors here are usually copy-pasting blindly or generating one giant monolithic block.",
28: "Password \"complexity\" on this exam means mixing character classes \u2014 upper/lower case, digits, AND symbols with no obvious word pattern \u2014 not just length.",
29: "lsof lists open files by process; piping it to a mount point's name reveals exactly what's still using a filesystem before you force an unmount.",
30: "Playbook = the automation script itself (tasks + config) that gets applied to hosts \u2014 don't confuse it with Inventory (which hosts to run against).",
31: "Securing a departing/long-leave account means locking the password (passwd -l, prepends ! in /etc/shadow) AND setting the shell to /sbin/nologin \u2014 deleting their home directory or waiting on future logins doesn't secure anything.",
32: "A variable only survives into new shell sessions if it's written into a startup file like ~/.bashrc \u2014 export alone in the current shell doesn't persist after logout.",
33: "Password-history/expiry rules exist mainly to stop users from cycling back to an old, possibly already-exposed password.",
34: "qemu-img convert changes a virtual disk's format (e.g. qcow2 \u2194 raw) \u2014 qemu-kvm/qemu-io/qemu-ng aren't real conversion subcommands, they're distractors built from real qemu tool names.",
35: "When a scenario asks what to do *first* with AI tooling, the safe CompTIA answer is almost always \"check the organization's AI usage policy\" before using any tool.",
36: "Among pull/reset/rebase/stash, only git reset actually moves the branch pointer backward and discards the broken commits \u2014 pull fetches+merges, rebase replays commits elsewhere, stash only shelves changes temporarily.",
37: "rm -r recursively removes a directory and everything inside it; rmdir only removes empty directories.",
38: "docker ps lists running containers (add -a to include stopped ones) \u2014 the way to verify containers are actually up.",
39: "modinfo <module> shows a kernel module's version/description/parameters; modprobe loads a module, lsmod only lists what's currently loaded (no version info), depmod rebuilds dependency maps.",
40: "git stash shelves uncommitted local changes so you can pull cleanly, then `git stash pop` brings them back afterward \u2014 that round-trip is the giveaway phrase \"save for later review.\"",
41: "tar's compression flag picks the algorithm: -z = gzip, -j = bzip2, -J = xz \u2014 match the flag to the extension named in the question (.tar.xz here).",
42: "A job backgrounded with nohup + & is still a background job \u2014 fg brings the most recent background job into the foreground of the current terminal.",
43: "systemd-analyze blame ranks every unit by how long it took to start during boot \u2014 the tool for \"how long did service X take to start.\"",
44: "ufw's allow/deny syntax takes a port/protocol (e.g. 53/tcp), not a service alias like dns/tcp, and \"enable\"/\"disable\" toggle the whole firewall rather than adding a rule.",
45: "PEP 8 is Python's official style guide (naming, indentation, line length) \u2014 not a package tool or a language feature.",
46: "git init creates a brand-new local repository; git clone copies an existing remote one \u2014 pick init when the repo doesn't exist yet anywhere.",
47: "net.ipv4.ip_forward is the kernel switch that turns a Linux box into a router by forwarding packets between interfaces \u2014 the other ip_* parameters here are decoys with plausible-sounding names.",
48: "Adding a directory to $PATH (export PATH=$PATH:/dir) lets you invoke scripts there by name alone, without ./ or a full path \u2014 always append to the existing PATH, don't overwrite it.",
49: "systemctl list-units --type=service enumerates every service unit and its current status in one shot; is-active/is-enabled only check a single unit you already know the name of.",
50: "LUKS (Linux Unified Key Setup) is full-disk/block-device encryption \u2014 GPG and OpenSSL encrypt files/data streams, and PKI certificates are for identity, not at-rest disk encryption.",
51: "A webhook is just an HTTP callback \u2014 an event triggers an outbound HTTP request to a URL you registered, nothing more exotic than that.",
52: "yum/dnf repolist all shows every configured repo (enabled and disabled) with its status \u2014 the command to confirm a repo is actually installed and enabled before troubleshooting further.",
53: "Password expiry policies exist mainly to limit how long a leaked/exposed password stays valid, not to force complexity or eliminate passwords outright.",
54: "vmstat/uptime showing a load average far above the core count (nproc) with idle CPU pegged near 0 points to a CPU-bound bottleneck \u2014 the fix is more CPU, not more memory/swap/disks.",
55: "When a firewall has an explicit DENY rule for a port that's already open and listening, delete that specific deny rule (ufw delete deny ...) rather than broadly allowing more traffic or disabling the firewall.",
56: "The immutable attribute (chattr +i) blocks writes even for root and shows as \"i\" in lsattr's output \u2014 clear it with chattr -i before any chmod/chown will let you edit the file.",
57: "curl/dig failures like \"could not resolve host\" or NXDOMAIN on a *known-good* domain point at the client's own DNS configuration (resolv.conf), not the remote server or the network link.",
58: "\"Permission denied\" running a script that already has +x can mean the filesystem itself is mounted noexec \u2014 check `mount` output before assuming it's a chmod problem, and remount with -o remount,exec.",
59: "Test operators: -eq/-ne compare equality, -gt/-lt compare magnitude \u2014 \"appears zero times\" is an equality check, so -eq 0 is what you want, not a greater/less-than comparison.",
60: "A service can be listening locally (visible in ss -lnt) and still be unreachable remotely if the host firewall's service list doesn't include it \u2014 check firewall-cmd --list-all's services line, not just whether the port is bound.",
61: "SELinux can silently block SSH key auth even when file *permissions* are correct \u2014 an authorized_keys file with the wrong SELinux context needs restorecon, not a permissions or service-restart fix.",
62: "An open telnet port on a scan is a security finding by itself (unencrypted remote access) \u2014 the fix is disabling/removing the service, not just firewalling the port.",
63: "TLS setup errors that say a key/cert \"cannot be loaded\" are often a path problem, not a content problem \u2014 check that the private key actually lives in the directory the service config points to.",
64: "A high load average alongside slow, unresponsive simple commands (even ones that don't touch disk) points to CPU contention rather than I/O wait or just \"high uptime.\"",
65: "journalctl / dmesg entries mentioning \"oom-killer\" and \"invoked\" mean the kernel's out-of-memory killer stepped in because a process (or the whole system) ran out of RAM.",
66: "A DNS query that times out with \"no servers could be reached\" means the queried server itself is unreachable/down \u2014 that's different from an NXDOMAIN (\"name doesn't exist\") response.",
67: "Running a read-only/query command like `groups` with sudo shows *root's* group membership, not the user you actually changed \u2014 always re-check as the target user, not as root.",
68: "/bin holds core executable binaries and utilities available to all users \u2014 distinct from /etc (config), /var (variable data/logs), and /sys (kernel/device interface).",
69: "Under SELinux enforcing, a service refusing to bind to a non-standard port usually needs that port added to the right SELinux port type (semanage port -a -t <type>_port_t -p tcp <port>) \u2014 disabling enforcement or unmasking the unit skips the actual fix.",
70: "shred is the secure-delete tool of choice \u2014 it overwrites data before removal so it can't be recovered, unlike a plain rm/unlink which only removes the directory entry.",
71: "ss -an | grep <port> shows whether a port is listening/established locally \u2014 it's the modern, more reliable replacement for netstat-style port checks.",
72: "sed -i 's/old/new/' file does an in-place find-and-replace \u2014 the direct tool for swapping one string for another inside a file, versus piecing it together with cut/grep/awk.",
73: "In a Dockerfile, ENTRYPOINT defines the command that runs when the container starts; FROM sets the base image, ONBUILD defers instructions to child builds, and EXPOSE just documents a port.",
74: "Python virtual environments isolate each project's package versions from one another \u2014 the point is avoiding version conflicts between projects, not saving disk space or sharing packages globally.",
75: "Kerberos is the ticket-based authentication protocol Linux uses to integrate with a Windows Active Directory domain \u2014 Samba handles the file/print sharing side, not authentication itself.",
76: "pam_tally2 (and its successor pam_faillock) tracks failed login attempts and locks accounts after a threshold \u2014 the PAM module built specifically to blunt dictionary/brute-force attacks.",
77: "Ansible's core identity: agentless automation driven by YAML playbooks \u2014 not a CI/CD pipeline tool and not built on a Ruby DSL (that's more a description of Chef/Puppet-style tools).",
78: "After hand-editing any systemd unit or timer file, systemctl daemon-reload is required before the change takes effect \u2014 systemd won't pick up file edits on its own.",
79: "find combined with -type d restricts the operation to directories only, and 700 keeps them accessible to the owner alone \u2014 watch for scope (~) vs (/home vs $HOME) matching what the question actually asked for.",
80: "apt-mark hold pins a package at its current version so future upgrades skip it (dpkg --set-selections is the older, more manual equivalent) \u2014 the tool for \"keep this package from being upgraded.\"",
81: "diff's compact output format (2,3d1 / 4a3, with < and >) is its own notation for line-by-line differences between two files \u2014 recognizing that format is the key to this question type.",
82: "When new files in a shared directory need to inherit the directory's group automatically, set the setgid bit on the directory (chmod g+s) rather than fixing group ownership after the fact each time.",
83: "\">>\" appends a command's output to the end of an existing file without erasing what's already there; a single \">\" would overwrite it instead.",
84: "A \"can't reach anything by name\" network error with routing/interface otherwise fine usually traces back to a missing or misconfigured DNS resolver, not the default route or netmask.",
}

applied = 0
for idx, tip in TIPS.items():
    if idx in byidx:
        byidx[idx]["examTip"] = tip
        applied += 1

json.dump(bank, open(PATH, "w", encoding="utf-8"), indent=1, ensure_ascii=False)
print("applied", applied, "tips; total with tips now:", sum(1 for q in bank if q.get("examTip")))
